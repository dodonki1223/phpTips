# 文字列が回文かどうか判定する

## 回文（逆から読んでも同じ文字）かどうかを判定するメソッド

### 実行結果

```php
$ php IsPalindrome.php
<回文かどうかの確認>
aCa：bool(true)
acc：bool(false)
01234567899876543210：bool(true)
stressedesserts：bool(true)
strEssedesserts：bool(false)
```
